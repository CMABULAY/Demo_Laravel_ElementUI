<template>
    <h6>
        *note that this website is using composer and node.js, you must download/install first before running this application, then proceed to the ff. <br>
        
        1. open new terminal then input - Npm install<br>
        2. open new terminal then input - Composer install <br>
        3. set your database .env file.<br>
        4. open new terminal then input - php artisan jwt:secret 
        5. then php artisan migrate for the tables. :) 
    </h6>
</template>